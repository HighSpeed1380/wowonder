<?php 
if ($wo['WondredLikedusers']['anonymous'] == true) {
	$anonymous = true;
   $wo['WondredLikedusers']['username'] = 'anonymous';
   $wo['WondredLikedusers']['name'] = $wo['lang']['anonymous'];
   $wo['WondredLikedusers']['avatar'] = Wo_GetMedia('upload/photos/incognito.png');
   $wo['WondredLikedusers']['verified'] = 0;
   $wo['WondredLikedusers']['type'] = '';
 } ?>
<div class="who_react_to_this_user">
	<div class="who_react_to_this_user_info">
		<div class="avatar <?php echo Wo_RightToLeft('pull-left');?>" id="inline_emo_react">
			<?php if (!isset($anonymous)) { ?>
			<a href="<?php echo Wo_SeoLink('index.php?link1=timeline&u=' .  $wo['WondredLikedusers']['username'] . '');?>" data-ajax="?link1=timeline&u=<?php echo $wo['WondredLikedusers']['username'];?>">
			<img src="<?php echo $wo['WondredLikedusers']['avatar'];?>" alt="<?php echo $wo['WondredLikedusers']['name']; ?> Profile Picture" />
			<?php if( isset( $wo['WondredLikedusers']['reaction'] ) ){?>
				<span class="like-emo wo_who_react_this">
					<?php
					if (!empty($wo['reactions_types'][$wo['WondredLikedusers']['reaction']]['sunshine_small_icon'])) {
		                echo '<div class="inline_post_emoji inline_act_emoji"><img src="' . $wo['reactions_types'][$wo['WondredLikedusers']['reaction']]['sunshine_small_icon'] . '"></div>';
		            }
						// switch (strtolower($wo['WondredLikedusers']['reaction'])) {
						// 	case 'like':
						// 		echo '<div class="inline_post_emoji inline_act_emoji"><img src="' . $wo['config']['theme_url'] . '/reaction/like-sm.png"></div>';
						// 	break;
						// 	case 'love':
						// 		echo '<div class="inline_post_emoji inline_act_emoji"><img src="' . $wo['config']['theme_url'] . '/reaction/love-sm.png"></div>';
						// 	break;
						// 	case 'haha':
						// 		echo '<div class="inline_post_emoji inline_act_emoji"><img src="' . $wo['config']['theme_url'] . '/reaction/haha-sm.png"></div>';
						// 	break;
						// 	case 'wow':
						// 		echo '<div class="inline_post_emoji inline_act_emoji"><img src="' . $wo['config']['theme_url'] . '/reaction/wow-sm.png"></div>';
						// 	break;
						// 	case 'sad':
						// 		echo '<div class="inline_post_emoji inline_act_emoji"><img src="' . $wo['config']['theme_url'] . '/reaction/sad-sm.png"></div>';
						// 	break;
						// 	case 'angry':
						// 		echo '<div class="inline_post_emoji inline_act_emoji"><img src="' . $wo['config']['theme_url'] . '/reaction/angry-sm.png"></div>';
						// 	break;
						// 	default:
						// }
					?>
				</span>
			<?php } ?>
			</a>
		<?php }else{ ?>
			<img src="<?php echo $wo['WondredLikedusers']['avatar'];?>" alt="<?php echo $wo['WondredLikedusers']['name']; ?> Profile Picture" />
		<?php } ?>
		</div>
		<div>
			<?php if (!isset($anonymous)) { ?>
			<span class="user-popover views_info_count" data-id="<?php echo $wo['WondredLikedusers']['id']; ?>" data-row-id="<?php echo $wo['WondredLikedusers']['row_id']; ?>" data-type="<?php echo $wo['WondredLikedusers']['type']; ?>">
				<a href="<?php echo Wo_SeoLink('index.php?link1=timeline&u=' .  $wo['WondredLikedusers']['username'] . '');?>" data-ajax="?link1=timeline&u=<?php echo $wo['WondredLikedusers']['username'];?>"><p><?php echo $wo['WondredLikedusers']['name']; ?></p></a>
			</span>
			<?php }else{ ?>
				<p><?php echo $wo['WondredLikedusers']['name']; ?></p>
			<?php } ?>
		</div>
	</div>
	<div class="user-follow-button">
		<?php if ($wo['WondredLikedusers']['anonymous'] != true) {
		echo Wo_GetFollowButton($wo['WondredLikedusers']['user_id']);
	}?>
	</div>
</div>